# surfriderpools - Project Rules

## Style Rules
- Never use em dashes or hyphens in sentences. Use a comma instead. Hyphens are only allowed when hyphenating a compound word (e.g., "long-term") or between numbers (e.g., "2020-2025").

## Site Facts
- Read SITE_FACTS.md before making content changes.

## Build / Deploy
- App lives in `site/`. Vercel Root Directory is `site`.
- GitHub repo: `CalTech-Web/surfriderpools`.
- Vercel project: `surfriderpools` under scope `brandon-7928s-projects` -> surfriderpools.vercel.app
- Contact form posts to forms.caltechweb.com (site key `surfriderpools.com`).

# currentDate
Today's date is 2026-06-19.
